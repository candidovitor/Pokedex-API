""" types = TypesModel.find_by_id(type_id)
        types = types.json()
        type_id = types['type_id']

        return {type_id: [x.json() for x in attacks]}"""

{
    "attack_name": "Attack bite of the dragon",
    "damage": 8000,
    "type_id": 20
}